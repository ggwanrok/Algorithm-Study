#include <bits/stdc++.h>

using namespace std;

int main(){
    int n; cin>>n;
    while(true){
        if(n <= 0) break;
        cout<<"long ";
        n -= 4;
    }
    cout<<"int\n";
    return 0;
}